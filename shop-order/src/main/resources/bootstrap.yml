spring:
  application:
    name: shop-order
  cloud:
    nacos:
      config:
        server-addr: 127.0.0.1:8848 #nacos中心地址
        file-extension: yml # 配置文件格式
        shared-configs: shop-database-dev.yml # 公共配置文件
  profiles:
    active: dev # 环境标识
